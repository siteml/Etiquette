# etiq print — engine (planned)

Successor to the internal `labelprint` tool (Go, single exe, Win32 GUI,
Epicor REST, RAW/driver/PDF output). This directory will receive the
generalized engine once the Etiquette SVG convention resolver is in place.

Design notes: docs/convention.md (PRINTING section), docs/roadmap.md Phase 3.
